long long branches_test(int event, int num_runs,
			long long *high,
                        long long *low);

long long random_branches(int event, int num_runs,
			  long long *high,
			  long long *low);
